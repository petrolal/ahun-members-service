spring:
  devtools:
    restart:
      enabled: false

  application:
    name: ahun-members-service

  datasource:
    url: jdbc:postgresql://db.phhqoebohkenvvdciapc.supabase.co:5432/postgres?sslmode=require&preferIPv4Stack=true
    username: postgres
    password: 07NdvHA6oGadn9dQ
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5

  jpa:
    hibernate:
      ddl-auto: none

telegram:
  bot-token: ${TELEGRAM_BOT_TOKEN}
  chat-id: ${TELEGRAM_CHAT_ID}

google:
  credentials: ${GOOGLE_CREDENTIALS}

